//  import { shallowMount } from '@vue/test-utils'
//  import Empresas from '@/components/Empresas.vue'
//  import Empresas from '../../src/components/Empresas.vue'

//  const expect = global.expect

/*  describe('Empresas.vue', () => {
    it('testing Empresas', () => {
      const wrapper = shallowMount(Empresas)
      const message = wrapper.find('h1').text()
      expect(message).toBe('Empresas')
    })
})  */
/*  describe('empresas',() => {
  const wrapper = shallowMount(empresa, {
    propsData: {
      variasEmpresas : [
      {
        id: 12,
        name: "ejemplo 12",
        value: 105000
      },
      {
        id: 13,
        name: "ejemplo 13",
        value: 105001
      },
      {
        id: 14,
        name: "ejemplo 14",
        value: 105002
      }
    ]
    }
})

  test('Debe ordenar de mayor a menor', () => {
    expect(Empresas.sortHighest(empresa[0], empresa[2]))
  })

  })  */
